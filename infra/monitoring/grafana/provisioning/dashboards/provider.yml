apiVersion: 1

providers:
  - name: "Route Management Dashboards"
    orgId: 1
    folder: "Route Management"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
