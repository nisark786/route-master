apiVersion: v1
kind: Service
metadata:
  name: {{ include "route-management.fullname" . }}-postgres-ai
  namespace: {{ include "route-management.namespace" . }}
  labels:
    {{- include "route-management.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "route-management.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgres-ai
  ports:
    - name: postgres
      port: {{ .Values.postgresAi.service.port }}
      targetPort: postgres
---
